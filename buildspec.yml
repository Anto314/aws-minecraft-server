version: 0.2
phases:
  install:
    commands:
      - yum install gcc git -y
      - echo "Download mcron source code"
      - git clone https://github.com/Tiiffi/mcrcon.git
      - cd mcron
      - make
      - cd - 
      - cp mcron/mcron /userdata/files/mcron
artifacts:
  files:
    - "userdata/**/*"